# Changesets

This folder holds [changesets](https://github.com/changesets/changesets) — one Markdown file
per pending change describing the version bump (patch/minor/major) and a summary. The Release
workflow consumes them to version and publish the package.

Add one with:

```bash
npx changeset
```
